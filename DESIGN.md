# DESIGN.md — にほんご Pocket

폰 우선 일본어 독학 PWA의 디자인 시스템. 색·간격·컴포넌트는 `src/styles.css`의
CSS 변수가 단일 출처(single source of truth)다. 새 화면은 새 색/패턴을 만들지 말고
아래 토큰과 컴포넌트를 재사용한다.

## 원칙
- **모바일 우선, 한 손 조작.** `.app` 폭 480px, 세로 레이아웃. 탭 타깃 최소 44–48px.
- **다크 테마 고정.** 라이트 모드 없음.
- **즉각 피드백 루프.** 정답/오답은 색 + 아이콘(✓/✗) 둘 다로 — 색만으로 의미를 전달하지 않는다(색맹/WCAG).
- **뺄셈 우선.** 장식 요소 금지. 모든 요소는 픽셀값을 한다.
- **상단 정렬 일관.** 모든 화면 상단 콘텐츠는 `--screen-top`(40px)에서 시작.

## 색 토큰 (`:root`)
| 토큰 | 값 | 용도 |
|---|---|---|
| `--bg` | `#0f1020` | 페이지 배경 |
| `--surface` | `#1b1c3a` | 카드·버튼·옵션 기본 면 |
| `--surface-2` | `#26284d` | 한 단계 밝은 면(선택 상태, 지문 박스) |
| `--primary` | `#7c5cff` | 주 액션·진행바·강조 |
| `--primary-press` | `#6647e6` | 주 버튼 눌림 그림자 |
| `--text` | `#f4f4ff` | 본문 |
| `--muted` | `#a4a6c8` | 보조 텍스트·라벨 |
| `--correct` | `#3ddc84` | 정답 |
| `--wrong` | `#ff5c7c` | 오답·약점 표시 |
| `--track` | `#14152b` | 빈 진행바 홈 |
| `--radius` | `16px` | 카드·버튼 모서리 |
| `--screen-top` | `40px` | 모든 화면 공통 상단 오프셋 |

색은 항상 위 변수로 참조한다. 하드코딩 hex 금지(정답/오답 안쪽 글자색 같은 대비용 예외만).

## 타이포
- 폰트: 시스템 스택(`system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif`). 일본어 글리프는 OS 폰트에 위임.
- 숫자가 흔들리면 `font-variant-numeric: tabular-nums`(진행 카운터·점수).
- 글리프 크기: `.glyph.big` 110px(가나·한자), `.glyph.word` 56px(단어), `.glyph.sentence` 30px(문장).

## 컴포넌트 어휘
| 클래스 | 역할 |
|---|---|
| `.screen` | 화면 컨테이너(상단 패딩 `--screen-top`, gap 20px) |
| `.card` | 중앙 정렬 콘텐츠 카드(레슨/시험 본문) |
| `.btn-primary` / `.btn-ghost` | 주/보조 버튼. 풀폭, 최소 56px |
| `.link` | 텍스트 아이콘 버튼(✕ 등), 48px 터치 타깃 |
| `.options` + `.opt` | 4지선다 그리드(2열). `ChoiceGrid` 컴포넌트가 렌더 |
| `.opt.correct` / `.opt.wrong` / `.opt.dim` / `.opt.selected` | 옵션 상태 |
| `.progress-bar` (+`.slim`) + `.progress-fill` (+`.weak`) | 진행/점수 막대 |
| `.modal-backdrop` + `.modal` | 확인 다이얼로그 |
| `.chip` / `.chips` | 작은 태그(문장 조각, 카테고리) |
| `.banner` | 경고/안내 배너 |
| `.sr-only` | 스크린리더 전용 |

**공유 컴포넌트:** 4지선다는 `ChoiceGrid`(`src/components/ChoiceGrid.tsx`)를 쓴다.
`mode="answer"`(채점 없이 선택)와 `mode="feedback"`(정답/오답 표시)를 지원 —
레슨과 JLPT 시험이 함께 사용. 새 선택지 UI를 따로 만들지 말 것.

## 인터랙션 상태(필수 명세)
새 기능은 다음을 모두 정의한다: 로딩 / 빈 상태 / 에러 / 성공 / 부분. 빈 상태는
"준비 중"처럼 비활성 + 안내 문구를 갖는다. 음성(TTS) 없는 기기 폴백을 항상 고려.

## 접근성
- 터치 타깃 ≥ 44px(주 버튼 56px, 아이콘 48px).
- 의미는 색 + 텍스트/아이콘 병기. 색 단독 금지.
- 오디오 버튼엔 `aria-label`, 무음 기기엔 텍스트 폴백.
- 피드백은 `role="status"` + `.sr-only`로 안내.

## 안티 패턴(피할 것)
- 새 임의 hex 색(토큰 안 쓰기).
- 장식용 블롭/그라데이션/이모지 떡칠.
- 색만으로 정답·약점 표시.
- 기존 컴포넌트 대신 비슷한 UI 재구현(특히 옵션 그리드).
